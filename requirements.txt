Pillow>=10.0.0
PyAutoGUI>=0.9.54
requests>=2.32.0
# Windows UI Automation accessibility tree for reliable click targeting.
# Windows-only; the agent degrades gracefully to VLM + Set-of-Marks if it is absent.
uiautomation>=2.0.18; platform_system == "Windows"
